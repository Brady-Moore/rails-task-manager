<h1><%= @task.title %></h1>
<%= link_to "Edit this task", edit_task_path %>

<p>
    <%= @task.details %>
</p>
<p>
    <%= check_box_tag "completed", "1", @task.completed, disabled: true %>

    <% if @task.completed %>
    This task is completed
<% else %>
    This task is not completed yet
<% end %>
    </p>
<%= link_to "Back to list", tasks_path%>
